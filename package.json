{
  "name": "finm--microservices",
  "version": "1.0.0",
  "description": "",
  "workspaces": {
    "packages": [
      "users-service",
      "auth-service",
      "balance-service"
    ]
  },
  "scripts": {
    "dev": "lerna bootstrap && lerna run dev --parallel",
    "test": "echo \"Error: no test specified\" && exit 1",
    "db:start": "docker-compose down && docker-compose up --build",
    "db:migrate": "(cd users-service && npm run db:migrate); (cd balance-service && npm run db:migrate);"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-personal:porithe/finm--microservices.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
